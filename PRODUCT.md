# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

เจ้าหน้าที่และครูในโรงเรียน ที่รับผิดชอบจัดการแผนปฏิบัติการ งบประมาณ และเอกสารประกอบ ล็อกอินเข้าระบบเพื่อบันทึก/ติดตามข้อมูลบุคลากรและเอกสารที่เกี่ยวข้องกับกลุ่มสาระการเรียนรู้ของตน (ภาษาไทย, คณิตศาสตร์, วิทยาศาสตร์และเทคโนโลยี, สังคมศึกษาฯ, สุขศึกษาและพลศึกษา, ศิลปะ, การงานอาชีพ, ภาษาต่างประเทศ, งานสนับสนุนการศึกษา).

## Product Purpose

ระบบติดตามจัดเก็บเอกสารแผนปฏิบัติการและงบประมาณของโรงเรียน — ให้เจ้าหน้าที่ล็อกอินเข้าถึงและจัดการข้อมูลบุคลากร (เพิ่ม/แก้ไข/ลบ/ค้นหา) พร้อมกลุ่มสาระ ตำแหน่ง และข้อมูลบัญชีผู้ใช้ที่ผูกกับแผนงานและงบประมาณของแต่ละคน.

## Operating Context

- ยืนยันตัวตนผ่าน Supabase (`src/lib/supabase.ts`, `src/lib/auth-context.tsx`) และบัญชี admin สำรอง (username/password คงที่) สำหรับเข้าใช้งานฉุกเฉิน
- หลังล็อกอิน ผู้ใช้เข้าสู่หน้าจัดการบุคลากร (`PersonnelPage`) แบบตาราง ค้นหา/เพิ่ม/แก้ไข/ลบรายชื่อ พร้อม dialog ฟอร์ม

## Capabilities and Constraints

- โครงสร้าง: root project (`src/`, ผูกกับ `App.tsx` จริง, ใช้งานจริงผ่าน Supabase) และ subfolder `personnel-app/` ซึ่งเป็น workspace แยกต่างหาก (pnpm) — ทั้งสองมีหน้า LoginPage ของตัวเอง และทั้งสองอยู่ในขอบเขตงานออกแบบนี้
- Stack: Vite + React + TypeScript + Tailwind CSS + shadcn/ui (radix-ui primitives), lucide-react icons — มีอยู่แล้วในโค้ดเดิม
- ชื่อโรงเรียน/หน่วยงานเต็ม ("ราชกัญญาฯ") ปรากฏในข้อความหัวเรื่องบางจุด — ยังไม่ยืนยันเป็น brand commitment ตายตัว ห้ามเปลี่ยนคำนี้เองโดยไม่ถาม

## Product Principles

- ความชัดเจนของข้อมูลราชการ/เอกสารสำคัญมาก่อนความหวือหวา — นี่คือเครื่องมือทำงาน (Operate) ไม่ใช่หน้า marketing
- ต้องอ่านง่ายสำหรับผู้ใช้ทุกวัย รวมถึงเจ้าหน้าที่ที่ไม่ถนัดเทคโนโลยี
- คงความสอดคล้องกันระหว่างสอง LoginPage แม้อยู่คนละ workspace
